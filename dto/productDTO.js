class ProductDTO {
    constructor({ _id, title, price, category }) {
        this.id = _id;
        this.title = title;
        this.price = price;
        this.category = category;
    }
}

export default ProductDTO;
